name: FTP Test
on:
  push:
    branches: master
jobs:
  web-deploy:
    name: 🚀 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4
      - name: 📂 Sync files
        uses: ./
        with:
          server: ftp.s2swebsolutions.com
          username: ${{ secrets.ftp_username }}
          password: ${{ secrets.ftp_password }}
          protocol: ftps
          server-dir: public_html/test-push-ftp
